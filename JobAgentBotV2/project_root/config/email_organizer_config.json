{
    "rules": [
        {
            "name": "Example Rule",
            "condition": "subject contains 'example'",
            "actions": [
                "move to folder 'ExampleFolder'"
            ]
        }
    ]
}